## How It Works
The program loads the CSV file.
It reads the first line as column headers.
It reads each remaining line as a data record.
The user types a query in the console.
The program parses the SELECT part.
If WHERE exists, the program filters matching rows.
The program prints the selected columns.